global:
  scrape_interval: 15s

scrape_configs:
  - job_name: zyangbot
    # The bot is reachable by service name on the internal compose network;
    # its metrics server listens on 3000.
    static_configs:
      - targets: ["bot:3000"]
